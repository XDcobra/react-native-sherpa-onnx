# Package libarchive sources for iOS and publish as GitHub Release.
# Release tag is read from third_party/libarchive_prebuilt/IOS_RELEASE_TAG.
# No compilation: only sources + headers are zipped for the pod to build.
#
# When to run manually (Actions --> Prebuilt - libarchive (iOS) Release --> Run workflow):
# 1. After updating third_party/libarchive_prebuilt/IOS_RELEASE_TAG or libarchive submodule.
# 2. When you want to publish a new libarchive iOS release (sources zip for npm/small package).
#
name: Framework - libarchive (iOS) Release

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package-libarchive-ios:
    runs-on: macos-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Read release tag
        id: tag
        run: |
          TAG_FILE=third_party/libarchive_prebuilt/IOS_RELEASE_TAG
          if [ ! -f "$TAG_FILE" ]; then
            echo "::error::Missing $TAG_FILE (add a line with the release tag, e.g. libarchive-ios-v3.8.5)"
            exit 1
          fi
          RELEASE_TAG=$(grep -v '^#' "$TAG_FILE" | grep -v '^[[:space:]]*$' | head -1 | tr -d '\r\n')
          if [ -z "$RELEASE_TAG" ]; then
            echo "::error::Empty release tag in $TAG_FILE"
            exit 1
          fi
          echo "release_tag=$RELEASE_TAG" >> $GITHUB_OUTPUT
          VERSION=$(echo "$RELEASE_TAG" | sed 's/^libarchive-ios-v//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Using release tag: $RELEASE_TAG (version: $VERSION)"

      - name: Get submodule versions
        id: submodules
        run: |
          echo "libarchive=$(git -C third_party/libarchive rev-parse HEAD 2>/dev/null || echo 'none')" >> "$GITHUB_OUTPUT"
          echo "zstd=$(git -C third_party/zstd rev-parse HEAD 2>/dev/null || echo 'none')" >> "$GITHUB_OUTPUT"

      - name: Cache libarchive + zstd prebuilts (iOS)
        uses: actions/cache@v4
        id: libarchive-ios-cache
        with:
          path: |
            third_party/zstd_prebuilt/ios
            third_party/libarchive_prebuilt/ios
            third_party/libarchive_prebuilt/libarchive.xcframework
          key: libarchive-ios-prebuilt-${{ steps.submodules.outputs.libarchive }}-${{ steps.submodules.outputs.zstd }}-${{ hashFiles('third_party/libarchive_prebuilt/build_libarchive_ios.sh', 'third_party/zstd_prebuilt/build_zstd_ios.sh') }}

      - name: Build zstd (iOS)
        if: steps.libarchive-ios-cache.outputs.cache-hit != 'true'
        run: |
          cd third_party/zstd_prebuilt
          chmod +x build_zstd_ios.sh
          ./build_zstd_ios.sh

      - name: Build libarchive iOS XCFramework
        if: steps.libarchive-ios-cache.outputs.cache-hit != 'true'
        run: |
          chmod +x third_party/libarchive_prebuilt/build_libarchive_ios.sh
          third_party/libarchive_prebuilt/build_libarchive_ios.sh

      - name: Create release zip
        run: |
          cd third_party/libarchive_prebuilt
          zip -r ../../libarchive-ios.zip libarchive.xcframework
          cd ../..
          ls -la libarchive-ios.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          name: libarchive iOS ${{ steps.tag.outputs.release_tag }}
          make_latest: false
          body: |
            Prebuilt libarchive XCFramework for iOS.
            - **libarchive-ios.zip** – Contains `libarchive.xcframework` (arm64, x86_64).
          files: libarchive-ios.zip
          draft: false
          prerelease: false
          skip_duplicate: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          echo "## libarchive iOS release" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Release:** [${{ steps.tag.outputs.release_tag }}](https://github.com/${{ github.repository }}/releases/tag/${{ steps.tag.outputs.release_tag }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Asset | Description |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------------|" >> $GITHUB_STEP_SUMMARY
          echo "| \`libarchive-ios.zip\` | XCFramework containing libarchive for iOS. |" >> $GITHUB_STEP_SUMMARY
