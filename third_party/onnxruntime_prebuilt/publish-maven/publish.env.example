# Maven publish env — used by publish-to-github-pages.sh and by
# .github/workflows/build-onnxruntime-android-release.yml
#
# Set these in the workflow (or export before running the script):
#   MAVEN_VERSION  — e.g. 1.17.1-qnn2.43.1.260218 (from VERSIONS: ONNXRUNTIME_VERSION-qnnQNN_SDK_VERSION)
#   AAR_SRC        — path to the built AAR, e.g. onnxruntime-android-qnn.aar (relative to workspace root when script runs)
#   MAVEN_REPO_PAT — GitHub PAT with push access to the Maven repo (e.g. XDcobra/maven)
#
# Fixed values for this artifact (edit here if coordinates change):

GROUP_ID=com.xdcobra.sherpa
ARTIFACT_ID=onnxruntime
