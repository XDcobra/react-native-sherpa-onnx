export const RELEASE_API_BASE =
  'https://api.github.com/repos/k2-fsa/sherpa-onnx/releases/tags';
export const CACHE_TTL_MINUTES = 24 * 60;
/** Default batch size for native `batchTtsCatalogHints` when refreshing the TTS model list (JS-side chunking). */
export const DEFAULT_TTS_CATALOG_HINTS_CHUNK_SIZE = 256;
export const MODEL_ARCHIVE_EXT = '.tar.bz2';
export const MODEL_ONNX_EXT = '.onnx';
