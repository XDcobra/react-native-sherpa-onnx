# Source separation model detection tests: C++ tests vs source-separation-models-expected.csv.
# Triggered on PR (via test-model-detect) or manually.

name: Test – Source separation model detection

on:
  workflow_dispatch:
  workflow_call:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-separation-detect
  cancel-in-progress: true

jobs:
  separation-model-detect:
    name: Source separation model detection (C++)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build deps
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential libgtest-dev

      - name: Configure and build
        run: |
          mkdir -p test/cpp/build && cd test/cpp/build
          cmake .. -DCMAKE_BUILD_TYPE=Release
          cmake --build .

      - name: Run source separation model detection tests
        working-directory: ${{ github.workspace }}
        run: |
          ./test/cpp/build/model_detect_test \
            --gtest_filter=ModelDetectTest.FixturesExist:ModelDetectTest.DetectSeparationFromFileListMatchesExpected:ModelDetectValidation.Separation*
