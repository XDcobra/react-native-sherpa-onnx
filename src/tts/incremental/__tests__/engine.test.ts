/**
 * @todo Rewrite tests for new pipeline-based API.
 * This test file was written for the legacy event-based `generateIncrementalSpeechStream` API.
 * The new API uses `startSession()` with LiveTextBuffer + TtsPipelineHandle model.
 * Tests need to be updated to reflect this architectural change.
 */
// TODO: Import and test the new API once refactored
// import { createEngine } from '../engine';
// import type { StreamingTtsEngine } from '../../streamingTypes';
