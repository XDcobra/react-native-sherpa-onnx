# Maven publish env — used by publish-to-github-pages.sh and by
# .github/workflows/build-sherpa-onnx-android-release.yml
#
# Set these in the workflow (or export before running the script):
#   MAVEN_VERSION  — e.g. 1.12.24 (from ANDROID_RELEASE_TAG, e.g. sherpa-onnx-android-v1.12.24 -> 1.12.24)
#   AAR_SRC        — path to the built AAR, e.g. sherpa-onnx-1.12.24.aar (relative to workspace root when script runs)
#   MAVEN_REPO_PAT — GitHub PAT with push access to the Maven repo (e.g. XDcobra/maven)
#
# Optional: publish Java API AAR with classifier "java" (same version, second artifact).
#   AAR_SRC_JAVA   — path to Java API AAR, e.g. sherpa-onnx-1.12.24-java.aar; if set and file exists, published as artifactId-version-java.aar
#
# Optional (modular / Best Practice): declare dependency on ONNX Runtime so consumers get it transitively.
#   DEPENDENCY_GROUP_ID    — e.g. com.xdcobra.sherpa
#   DEPENDENCY_ARTIFACT_ID — e.g. onnxruntime
#   DEPENDENCY_VERSION     — e.g. 1.24.2-qnn2.43.1.260218 (must match published onnxruntime version)
#
# Fixed values for this artifact (edit here if coordinates change):

GROUP_ID=com.xdcobra.sherpa
ARTIFACT_ID=sherpa-onnx
